{
  "name": "proyecto-personal",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx live-server docs --port=3000 --no-browser",
    "deploy": "node build.js && git add docs && git commit -m 'build: actualizar sitio' || echo 'sin cambios' && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "ejs": "^3.1.10",
    "fs-extra": "^11.3.2",
    "glob": "^11.0.3",
    "live-server": "^1.2.2"
  }
}
